BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[user_tb] ALTER COLUMN [userPassword] NVARCHAR(max) NOT NULL;

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
